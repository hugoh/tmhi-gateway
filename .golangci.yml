version: "2"
linters:
  default: all
  disable:
    - depguard
    - exhaustruct
    - exhaustruct_v5
    - gomodguard
    - noinlineerr
    - paralleltest
    - testpackage
    - wsl
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - err113
          - varnamelen
          - funlen
formatters:
  enable:
    - gci
    - gofumpt
    - goimports
    - golines
